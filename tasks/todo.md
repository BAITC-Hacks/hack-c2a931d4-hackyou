# TraceGraph AI — задачи реализации

Основание: `tasks/plan.md`. Объём: AI/ML-движок для интеграции другим инженером. T01–T18 завершены 23 сентября 2026 года; установку, CLI, SDK и проверки подтверждает `docs/validation.md`.

Указанные команды и пути — цели реализации. Задача закрывается по результату и короткой указанной проверке. На каждой задаче не требуется отдельный большой набор тестов.

## Этап 1 — контракт и достоверный вход

### T01. Создать устанавливаемый пакет и контракт v1

- [x] Подготовить пакет `tracegraph_ai` и зафиксировать схемы входа, результата, evidence и ошибок.
- [x] Определить строковые gid в JSON, nullable-поля, внутренние role_strength/confidence и экспорт role_score=confidence, версии и analysis_id.
- [x] Согласовать имена признаков и владение модулями для параллельной работы.

**Проверка:** пакет устанавливается в отдельную среду и импортируется; небольшой пример DTO сериализуется без NaN и изменения большого gid.

**Зависимости:** нет. **Размер:** M, до 5 файлов.

**Файлы:** `ml/pyproject.toml`, `ml/src/tracegraph_ai/{__init__,schemas,config}.py`, `docs/ai-engine-contract.md`.

### T02. Загрузить и проверить три Parquet

- [x] Проверять обязательные поля, dtype, уникальность gid/пар edges, конечные значения и ссылки на узлы.
- [x] Сверять пары, `sum_kzt` с точностью до тиына и `n_tx` между edges и transactions; сохранить повторяющиеся операции.
- [x] Выдавать сводку данных и понятную ошибку с названием файла/поля; исходные файлы не изменять.

**Проверка:** текущие 2 248/3 119/4 840 строк приняты; целевая порча суммы/ключа отклонена; большие gid точны. Размеры не хардкодятся для произвольного входа.

**Зависимости:** T01. **Размер:** M.

**Файлы:** `ml/src/tracegraph_ai/io.py`, `data/{nodes,edges,transactions}.parquet`.

### T03. Построить полный направленный граф и базовые потоки

- [x] Добавить все nodes, затем рёбра с суммой и количеством операций; сохранить 19 изолятов.
- [x] Рассчитать in/out degree, денежные объёмы, частоты и отношения с явными масками неполноты.
- [x] Обрабатывать seed balance, нулевой вход, depth=4 и сохранность идентификаторов.

**Проверка:** 2 248 узлов, 3 119 рёбер и 35 слабосвязных компонент; ручная сверка одной пары и изолята с исходными таблицами.

**Зависимости:** T02. **Размер:** M.

**Файлы:** `ml/src/tracegraph_ai/{graph,features_flow,engine}.py`.

### Контрольная точка A

- [x] Данные, деньги, направление графа и идентификаторы проходят сверку; базовые признаки доступны программно.

## Этап 2 — независимые группы признаков

### T04. Рассчитать происхождение от seed

- [x] Рассчитать reachable_seed_ids, seed_reach_count, min_seed_distance и seed_convergence_score через направленные обходы.
- [x] Исключить собственный seed из независимой конвергенции; различать отсутствие пути и нулевое расстояние до себя.
- [x] Кешировать обходы без перечисления всех путей.

**Проверка:** маленький ручной граф с двумя сходящимися seed и изолятом; несколько узлов реального графа сопоставлены с обходом.

**Зависимости:** T03. **Размер:** S.

**Файлы:** `ml/src/tracegraph_ai/features_seed.py`.

### T05. Рассчитать временные паттерны

- [x] Получить диапазоны активности, активные дни, burst, синхронный fan-in/fan-out и временной проход 0–2 дня.
- [x] Использовать transactions, сохраняя повторные строки; не выводить порядок внутри дня.
- [x] Возвращать численные признаки и основания на уровне дневных наблюдений.

**Проверка:** ручная последовательность по дням и одна реальная карточка; подтверждено участие transactions в итоговых признаках.

**Зависимости:** T02, T03. **Размер:** S.

**Файлы:** `ml/src/tracegraph_ai/features_temporal.py`.

### T06. Рассчитать топологию и сообщества

- [x] Вычислить PageRank, directed betweenness, upstream/downstream reach и связи между сообществами.
- [x] Построить проекцию с суммой встречных весов, выполнить Louvain с фиксированным seed, назначить кластер изолятам.
- [x] Сформировать статистику кластеров и устойчивую нумерацию для одного и того же входа.

**Проверка:** все узлы имеют cluster_id, внутренний оборот сверяется с edges; повторный расчёт с тем же seed воспроизводим.

**Зависимости:** T03. **Размер:** M.

**Файлы:** `ml/src/tracegraph_ai/{features_topology,communities}.py`.

### Контрольная точка B

- [x] Готова общая feature table из независимых T04/T05/T06; значения имеют определения и ограничения, известны времена расчётов.

## Этап 3 — первый полезный результат

### T07. Сформировать evidence и наблюдаемость

- [x] Создать идентифицируемые evidence с величиной, источником, связями и видом observation/inference/limitation.
- [x] Отразить границу depth=4, неполный seed balance и малое число наблюдений.
- [x] Обеспечить непустое объяснение для каждого узла, включая периферию и изоляты.

**Проверка:** значения трёх разных evidence сопоставлены с feature table; ограничение наблюдаемости доступно в JSON.

**Зависимости:** T04–T06. **Размер:** M.

**Файлы:** `ml/src/tracegraph_ai/{evidence,observability}.py`.

### T08. Вычислить шесть гипотез ролей и confidence

- [x] Ввести нормировки, настраиваемые критерии всех ролей и выбор сильнейшей допустимой гипотезы.
- [x] Отделить confidence от внутреннего role_strength/role_scores; выдавать ambiguity и secondary_role при близких оценках, документировать экспорт role_score=confidence.
- [x] Учесть независимость сигналов и ограничения: отсутствие исходящих у depth=4 не создаёт уверенный terminal.

**Проверка:** ручной разбор двух обычных узлов, seed и пограничного узла; scores конечны и в [0,1], критерии задокументированы.

**Зависимости:** T07. **Размер:** M.

**Файлы:** `ml/src/tracegraph_ai/{roles,confidence,config}.py`, `docs/role-methodology.md`.

### T09. Получить приоритеты и первые выгрузки

- [x] Рассчитать базовый priority с объяснимым вкладом сигналов и модификатором наблюдаемости; при отсутствии anomaly использовать документированное перераспределение весов.
- [x] Записать три обязательных CSV, численный evidence до 200 символов и содержательный Top-20; равенства разрешать детерминированно.
- [x] Записать analysis/graph bundles и сводки без модельных результатов, которые ещё не вычислены.

**Проверка:** ровно одна строка на каждый входной gid, корректные колонки, непустые кластеры и отсортированный Top-20; JSON читается стандартным парсером.

**Зависимости:** T08. **Размер:** M.

**Файлы:** `ml/src/tracegraph_ai/{ranking,exports,diagnostics,engine}.py`.

### Контрольная точка C

- [x] Первое ядро от трёх Parquet до объяснимых ролей, кластеров, ranking и экспортов работает целиком; результат полезен без нейромодели.

## Этап 4 — ранняя интеграция

### T10. Завершить публичные SDK и CLI

- [x] Реализовать analyze/get_summary/get_node/get_top_nodes/get_cluster/get_graph и CLI с input/output.
- [x] Зафиксировать ошибки, пути артефактов, schema_version, analysis_id, изоляцию сессий и точный JSON-контракт.
- [x] Добавить работающий пример для интегратора: анализ → Top-20 → карточка → граф, и обновить описание контракта.

**Проверка:** `python -m tracegraph_ai --input ./data --output ./out` и пример SDK получают согласованные результаты; неверный gid обрабатывается явно.

**Зависимости:** T09. **Размер:** M.

**Файлы:** `ml/src/tracegraph_ai/{engine,__main__,__init__}.py`, `ml/examples/integrate.py`, `docs/ai-engine-contract.md`.

### Контрольная точка D

- [x] Инженер платформы может начать интеграцию через публичный контракт; последующие этапы добавляют рассчитанные поля без ломки схемы.

## Этап 5 — ML и качество приоритета

### T11. Ввести подготовку ML-признаков и IsolationForest

- [x] Собрать 15–25 признаков без gid, с log1p, масками пропусков и воспроизводимым scaler.
- [x] Реализовать IsolationForest как рабочий backend и будущий fallback; нормировать anomaly_score.
- [x] Передавать фактический backend, параметры и причины ограничений в model_info.

**Проверка:** полный набор конечных anomaly_score в [0,1]; raw gid не входит в матрицу; неизвестный pass-through не выдан за наблюдаемый ноль.

**Зависимости:** T10. **Размер:** M.

**Файлы:** `ml/src/tracegraph_ai/{anomaly_features,anomaly}.py`, `ml/pyproject.toml`.

### T12. Добавить CPU Autoencoder и объяснение аномалий

- [x] Реализовать архитектуру input→16→8→4→8→16→input, early stopping, ограничение эпох/времени и фиксированный seed.
- [x] Считать reconstruction error и основные признаки, давшие ошибку; не использовать модель для присвоения ролей.
- [x] При недоступности torch или ошибке обучения переходить на IF с явной записью причины.

**Проверка:** один успешный CPU-прогон AE и один принудительный fallback; оценка аномалии не подменяет роль и confidence.

**Зависимости:** T11. **Размер:** M.

**Файлы:** `ml/src/tracegraph_ai/{autoencoder,anomaly,engine,config}.py`, `ml/pyproject.toml`.

### T13. Уточнить ranking через counterfactual top-N

- [x] Выбрать top 20–30 предварительного рейтинга с учётом anomaly и смоделировать удаление каждого на копии графа.
- [x] Сравнить компоненты, размер крупнейшей, достижимость и связь с seed; сохранить measured/not_computed.
- [x] Добавить evidence разрушения связей и один раз пересчитать роли/confidence/priority, сохранив исходный граф; не повышать уверенность в роли только из-за anomaly.

**Проверка:** очевидный мост на небольшом графе даёт измеримое изменение; основной граф не изменён, число удалений ограничено N.

**Зависимости:** T12, T06, T09. **Размер:** M.

**Файлы:** `ml/src/tracegraph_ai/{counterfactual,roles,ranking,engine}.py`.

### T14. Сформировать аналитическую диагностику

- [x] Описать распределения ролей/аномалий, coverage, ambiguity, защиту depth=4, top-примеры и вклад признаков AE.
- [x] Проверить связи anomaly с оборотом/depth/seed; выдать предупреждение о вырожденном сигнале.
- [x] Измерить Top-20 overlap и rank correlation при детерминированном изменении основных весов примерно на ±10%.

**Проверка:** validation_report и model_info заполнены фактическими результатами; отсутствуют выдуманные accuracy/F1 и нечисловые значения.

**Зависимости:** T13. **Размер:** M.

**Файлы:** `ml/src/tracegraph_ai/{diagnostics,exports,engine}.py`.

### Контрольная точка E

- [x] ML-расширение и fallback работают, итоговый priority объясним, фактическая длительность укладывается в бюджет полного запуска.

## Этап 6 — объяснимое расследование

### T15. Выбрать следующее полезное evidence

- [x] Реализовать BNE для семи разрешённых действий из спецификации, причину выбора и эвристическую оценку полезности.
- [x] Учитывать уже рассмотренные evidence и сохраняющуюся неоднозначность; не выдавать повторный факт за новый.
- [x] При границе наблюдения рекомендовать конкретные недостающие данные, не имитируя их получение.

**Проверка:** consolidator/transit ambiguity ведёт к временному анализу, depth=4 — к разбору недостающих данных; отсутствие полезного шага возвращает stop.

**Зависимости:** T14. **Размер:** S.

**Файлы:** `ml/src/tracegraph_ai/next_evidence.py`.

### T16. Реализовать состояния и шаги расследования

- [x] Добавить start/continue, ветку на гипотезу, JSON-состояние и проверку принадлежности к analysis_id.
- [x] Ограничить автоматический запуск тремя шагами, явное продолжение одним шагом за вызов, всю ветку пятью шагами.
- [x] Сохранять reason, action, hypothesis/confidence до и после, evidence_found, next_action_reason и причину остановки.

**Проверка:** start → JSON round-trip → continue → предел 5; чужой/некорректный state отклоняется, повтор evidence не увеличивает confidence.

**Зависимости:** T15, T10. **Размер:** M.

**Файлы:** `ml/src/tracegraph_ai/{investigation,engine,schemas}.py`.

### T17. Подготовить отчёт и пример полного расследования

- [x] Сформировать детерминированный trace и отчёт: гипотеза, подтверждения, противоречия, confidence, ограничения, BNE и действия аналитика.
- [x] Обеспечить полезный ответ для любого узла, включая peripheral/изолят; слабые данные могут завершить ветку сразу.
- [x] Обновить пример и контракт: хранение состояния, восстановление соответствующего анализа, продолжение без LLM.

**Проверка:** пример API выполняет весь сценарий, числа отчёта совпадают с evidence, нет утверждений о доказанной виновности или выдуманных данных.

**Зависимости:** T16. **Размер:** M.

**Файлы:** `ml/src/tracegraph_ai/reports.py`, `ml/examples/integrate.py`, `docs/ai-engine-contract.md`.

### Контрольная точка F

- [x] Полный сценарий WHO / WHY / HOW SURE / HOW IMPORTANT / WHAT NEXT / HOW DID WE GET HERE работает без внешнего API.

## Этап 7 — готовность к передаче

### T18. Проверить воспроизводимость и оформить передачу

- [x] Выполнить чистую установку, полный CLI и SDK-пример; зафиксировать версии зависимостей, времена этапов и характеристики машины.
- [x] Собрать один компактный набор критических проверок: изолят, depth=4, seed balance, большие gid, повторы, fallback и лимиты ветки.
- [x] Завершить README, методологию ролей, схему решения, описание масштабирования и пятиминутный сценарий разбора 2–3 вычисленных узлов.

**Проверка:** 3 CSV + 4 JSON корректны, 2 248 узлов сохранены, evidence ≤200 символов, Top-20 отсортирован, запуск <5 минут; другой инженер следует инструкции без внутренних знаний проекта.

**Зависимости:** T17 и все предыдущие контрольные точки. **Размер:** M, преимущественно интеграция и документация.

**Файлы:** `README.md`, `ml/requirements-lock.txt`, `ml/tests/test_critical_cases.py`, `docs/role-methodology.md`, `docs/ai-engine-contract.md`.

### Контрольная точка G

- [x] Все критерии движка из `plan.md` выполнены и подтверждены результатами запуска.
- [x] Публичный контракт и артефакты переданы для подключения к платформе; готовность интерфейса оценивается его инженером отдельно.

## Что не входит в обязательный путь

LLM-пересказ, чат, внешний enrichment, GNN, supervised role classifier, GPU, полноценный backend/frontend, авторизация, постоянная БД и большой тестовый стенд. Возвращаться к дополнительным функциям после завершения согласованного движка и отдельного определения их ценности.

## Этап 8 — самостоятельный рабочий прототип 0.2.0

- [x] Связать временную координацию с конкретными совместимыми эпизодами; исключить независимые пики и обратную хронологию.
- [x] Возвращать ограниченные seed-пути, операции и ссылки от evidence; сохранять неизвестный порядок внутри дня.
- [x] Добавить get_transactions/get_subgraph с пагинацией, включительными датами и пересчётом локальных сумм при сохранении глобальных оценок узлов.
- [x] Сохранять и восстанавливать полный JSON-снимок с операциями, версиями и проверкой целостности без повторного обучения.
- [x] Выполнять целевые расчёты в расследовании, включая удаление выбранного узла вне Top-25 и локальный пересчёт гипотезы без изменения общего анализа.
- [x] Сохранять checkpoint и продолжать ветку на восстановленном экземпляре; ограничить 3 автоматическими / 5 общими шагами, изоляты завершать раньше.
- [x] Подготовить 30 случаев для аналитика и корректный отчёт по реальным отзывам, оставив отсутствующие оценки пустыми.
- [x] Проверить полный CLI, установленный wheel, сценарий SDK и 24 адресные проверки; обновить контракт и демонстрацию.

**Результат:** пакет 0.2.0, JSON schema 1.0, snapshot_version 1, investigation_version 2. Экспертная проверка гипотез подготовлена, но не подменена автоматическими метками. Подтверждённые измерения — в `docs/validation.md`.

## Этап 9 — обогащение расследования 0.3.0

- [x] Добавить самостоятельный `enrich_investigation()` с `EnrichmentConfig` и тремя параллельными локальными аналитическими агентами: пути, групповые формы, альтернативные объяснения.
- [x] Искать маршруты по совместимым датам, включая альтернативу топологически кратчайшему пути; проверять короткие возвраты и явно сохранять неизвестный порядок внутри дня.
- [x] Проверять концентрацию операций/контрагентов, временную чувствительность и ограничения наблюдаемости без изменения исходной роли, confidence или данных.
- [x] Ограничить работу квотами и кооперативным deadline; возвращать complete/partial/failed, диагностировать ошибки и усечение области.
- [x] Собирать досье с фактами, гипотезами, альтернативами, ссылками, пересечениями evidence и конкретными рекомендациями; сохранять минимальные подтверждающие операции при ограниченном реестре.
- [x] Поддержать загрузку снимков 0.2.0 из версии 0.3.0 без обучения и с сохранением версии производителя; кэшировать до восьми полных досье в экземпляре.
- [x] Подготовить пример экспорта JSON и читаемого Markdown-досье со ссылками, обновить README и контракт интегратора.
- [x] Выполнить оценку восьми сценариев с известным ответом и четырёх реальных узлов; отдельно описать знаменатели, сравнение с базовым объяснением, время и отсутствие экспертной разметки реальных случаев.
- [x] Проверить установленный wheel 0.3.0, полный CPU CLI, новый снимок, загрузку/сохранение 0.2.0 и продолжение старой ветки; завершить 31 адресную проверку и Ruff E9/F.

**Результат:** `enrichment_version=1`, локальное обогащение без LLM, внешних запросов и повторного обучения. Исходный анализ и пятишаговая ветка расследования сохраняются. Измеренные метрики и команда воспроизведения — в [docs/enrichment-metrics.md](../docs/enrichment-metrics.md); техническая проверка не объявляется точностью выявления нарушений.
