# Проверка рабочего прототипа 0.2.0

Дата: 23 сентября 2026 года. Проверен AI/ML-движок и его Python SDK на предоставленном датасете.

## Среда и установка

Windows x64, Python 3.12.0, Intel Core i7-7700HQ, 4 ядра / 8 логических потоков. CPU Autoencoder использует 2 потока; внешние API и GPU не нужны. Зависимости закреплены в `ml/requirements-lock.txt`.

Пакет 0.2.0 собран в wheel и установлен в отдельное `.venv-verify` с `--no-deps --no-build-isolation`; это обычная установка в site-packages. Проверен полный пример SDK с загрузкой снимка, сохранением в новый каталог и продолжением ветки после восстановления. CLI с полным обучением отдельно запущен из editable-окружения `.venv`.

```powershell
.\.venv\Scripts\python.exe -m tracegraph_ai --input data --output out-prototype-v02
.\.venv-verify\Scripts\python.exe ml/examples/integrate.py --load out-prototype-v02 --output out-prototype-installed
.\.venv\Scripts\python.exe ml/examples/review_results.py --analysis out-prototype-v02 --output out-review
```

## Измеренные результаты

| Результат | Значение |
| --- | --- |
| Полный CLI по внешнему секундомеру | 22,856 с |
| Таймер движка в этом запуске | 16,6087 с |
| Загрузка готового снимка без обучения | 1,590 с |
| Первые три целевых шага для выбранного узла вне Top-25 | 1,294 с |
| Модель | CPU Autoencoder |
| Узлы / рёбра / операции | 2 248 / 3 119 / 4 840 |
| Seed / изоляты / сообщества | 81 / 19 / 91 |
| Сверенный оборот | 365 890 012,01 KZT |
| Сохранённые похожие повторы | 97 сверх первого в группе |
| Снимок | 3 CSV + 5 JSON bundles + manifest.json |
| Неоднозначные роли | 133 |
| Случаи для аналитика | 30, метки пока не заполнены |

Время зависит от машины и данных. Таймер движка не включает первоначальные импорты и завершающее копирование результатов/обновление метаданных; внешний секундомер CLI учитывает весь процесс. Время загрузки измерено в отдельном SDK-процессе после импорта публичного пакета; это не время запуска всего Python-процесса.

Отпечаток аналитических карточек и кластеров:

```text
1696541e489cd629b906d61684ce31d55c8b70915bd6417e09b5530b8bd75f66
```

Установленный SDK восстановил этот же результат. Для лидера `100000003115284100` получены 3 seed-пути, подграф из 10 узлов и 19 локальных операций; ветка после восстановления завершилась на пяти шагах. Для `100000006274203100`, не входившего в исходный набор расчётов удаления, выполнен новый counterfactual и пересчёт гипотезы внутри ветки. В этом конкретном случае confidence сохранился: наличие нового расчёта не требует искусственного роста уверенности. Основной результат сессии остался неизменным.

## Адресные проверки

```powershell
.\.venv\Scripts\python.exe -m pytest ml/tests -q
.\.venv\Scripts\python.exe -m ruff check --select E9,F ml/src ml/examples ml/tests
```

Результат: **24 passed in 15.58s**, статические проверки прошли. Набор занимает около 16 секунд и проверяет существенные риски:

- точные большие gid, суммы в тиынах, повторяющиеся строки и сверку агрегатов;
- изоляты, границу глубины, неполный вход seed, fallback модели;
- несовместимые временные пики, обратный порядок дат, осторожную трактовку одного дня и ссылки эпизодов;
- восстановление без чтения Parquet/обучения, обнаружение повреждённого артефакта, продолжение ветки на восстановленном экземпляре;
- фильтрацию дат до обхода/агрегации, пагинацию операций, ограничение подграфа и прослеживаемость путей;
- целевой counterfactual, возможность изменения confidence на контролируемом примере, неизменность исходной сессии, подмену истории и пределы 3/5;
- раннее завершение изолята и отсутствие роста confidence от повторного расчёта известного evidence;
- пустые и частичные отзывы аналитика, допустимые метки и точные идентификаторы CSV.

Независимый обзор кода проверил семантику временных эпизодов, ссылок на операции, восстановления снимка и целевых расследований. Устранены фиктивный путь seed к самому себе и смешение модельных гипотез с наблюдениями в записи найденных путей.

## Что ещё требует реальных оценок

Роли и confidence остаются объяснимыми эвристиками. Разметки подтверждённых ролей и исходов в датасете нет. Подготовлен воспроизводимый разбор 30 случаев и отчёт по фактическим отзывам аналитика; пустой шаблон даёт `unreviewed`, а не оценку качества.

При обнулении вклада аномальности только в готовом рейтинге сохраняются 19 из 20 лидеров. Это локальная чувствительность итогового рейтинга: признаки, роли, confidence и исходный отбор узлов для counterfactual зафиксированы. Она не заменяет полный ablation и проверку полезности ML на размеченных случаях.

Интеграционный сценарий движка проверен через SDK. HTTP-адаптер, интерфейс, пользовательский доступ и хранение файлов в инфраструктуре платформы остаются её зоной ответственности. Каталог снимка содержит операции; передавать его следует в рамках доступа к исходной сессии.
